export declare const zipBundle: ({ distDirectory, buildDirectory, distDirectoryName, }: {
    distDirectory: string;
    buildDirectory: string;
    distDirectoryName: string;
}, withMaps?: boolean) => Promise<void>;
